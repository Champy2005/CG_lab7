# Workspace instructions

## Lab instruction in `instructions-1032980-17897008458766.pdf`

## Maintain the report's AI prompt log

The user requested that earlier, current and future prompts be recorded in
`AI_Prompt_log.md`. For each subsequent user prompt while working in this workspace:

1. Append a sequentially numbered entry in the AI prompt log in `AI_Prompt_log.md`,
   before the submission checklist. Preserve all existing entries and student edits.
2. Record the date and the user's task text verbatim, preserving line breaks.
   Do not log injected environment/tool metadata as user task text.
3. Include the assistant/model identity only to the extent known, the reason for
   asking (mark it as inferred if unclear), and the assistance provided.
4. Record actual assistant verification and leave student verification explicitly
   pending until the user supplies it. Never invent test results, measurements,
   acceptance or student actions.
5. Update the entry with the outcome before finishing the response. Log questions
   and follow-up requests as well as implementation requests, without duplicating
   prompts already recorded.

Keep the report a fillable template until the user supplies results or requests
completion. Follow any later user instruction to change or stop prompt logging.
